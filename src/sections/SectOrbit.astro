---
import mars from "../assets/img/section-4-mars.svg";
import phobos from "../assets/img/section-4-phobos.svg";
import deimos from "../assets/img/section-4-deimos.svg";
import ellipse from "../assets/img/ellipse.png";
import { Image } from "astro:assets";
---

<section>
    <div class="container">
        <h2>А еще у Марса есть спутники</h2>
        <div class="legend">
            <div class="item">
                <Image class="img" src={phobos} alt="phobos" />
                <p>Phobos</p>
            </div>
            <div class="item">
                <Image class="img" src={deimos} alt="deimos" />
                <p>Deimos</p>
            </div>
        </div>
        <div class="orbit">
            <Image class="mars" src={mars} alt="mars" />
            <Image class="ellipse" src={ellipse} alt="ellipse" />
            <div class="phobos">
                <Image class="img" src={phobos} alt="phobos" />
            </div>
            <div class="deimos">
                <Image class="img" src={deimos} alt="deimos" />
            </div>
        </div>
    </div>
</section>

<style lang="scss">
    section {
        background-color: #ffb762;
    }
    .container {
        position: relative;
        min-height: 100vh;
        display: flex;
        padding-block: 90px;
        flex-direction: column;
        justify-content: space-between;
    }
    .legend {
        display: flex;
        align-items: flex-end;
        gap: 30px;
        .item {
            display: grid;
            gap: 1rem;
            text-align: center;
            font-size: 20px;
            justify-items: center;
        }
    }
    .mars {
        position: absolute;
        border: 2px dashed #bbb;
        z-index: 3;
        top: calc(50% - 201px);
        left: calc(50% - 206px);
    }
    .phobos {
        width: 354px;
        z-index: 2;
        position: absolute;
        top: calc(50% - 48px);
        left: calc(50% - 354px);
        transform-origin: right;
        animation: rotate 25s linear infinite;
    }
    .deimos {
        width: 339px;
        z-index: 1;
        position: absolute;
        top: calc(50% - 30px);
        left: calc(50% - 336px);
        transform-origin: right;
        animation: rotate 15s linear infinite reverse;
        .img {
        }
    }
    .ellipse {
        z-index: 0;
        position: absolute;
        width: 618px;
        height: 618px;
        top: calc(50% - 309px);
        left: calc(50% - 309px);
    }
    .orbit {
        position: absolute;
        inset: 0;
        top: 120px;
    }
    h2 {
        line-height: 1;
        font-size: 60px;
        font-weight: 500;
        text-align: center;
    }
    @keyframes rotate {
        from {
            rotate: 0deg;
        }
        to {
            rotate: 360deg;
        }
    }
</style>
